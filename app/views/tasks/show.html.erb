<h1>task_id = <%= @task.id %> のタスク詳細</h1>

<table class="table table-hover">
  <tr>
    <th>task_id</th>
    <td><%= @task.id %></td>
  </tr>
  
  <tr>
    <th>タスク</th>
    <td><%= @task.content %></td>
  </tr>
  
  <tr>
    <th>ステータス</th>
    <td><%= @task.status %></td>
  </tr>
</table>

<%= link_to 'このタスクを管理する', edit_task_path(@task), class: 'btn btn-success' %>

<%= link_to 'このタスクを削除する', @task, method: :delete, data: { confirm: '本当に削除してよろしいですか？' }, class: 'btn btn-danger' %>
